<!-- login form -->
<html>

<body>
  <div class="card u-center-alt u-absolute bg-red-700 text-warning" style="width: 600px; height: 450px">
    <div class="card__header u-center bg-red-700 text-warning">
      <p class="font-bold px-3">Login</p>
    </div>
    {{!-- changed from div to form --}}
    <form class="content bg-red-700 text-warning login-form">
      <div class="form-group p-4">
        <input type="text" class="form-group-input" {{!-- changed to email and added id of 'email-login' --}}
          placeholder="E-mail" id="email-login" />
      </div>
      <div class="form-group p-4 bg-red-700 text-warning">
        <input {{!-- changed type to password, added id of 'password-login' --}} type="password"
          class="form-group-input" placeholder="Password" id="password-login" />
      </div>
      <div class="u-center">
        <a href="/signup" class="text-black text-bold">First time here? Become a Hot Shot!</a>
      </div>
      <div class="u-center bg-red-700 text-warning p-4">
        {{!-- added type submit to button --}}
        <button type="submit" class="btn-link outline bg-black text-warning">Submit</button>
      </div>
    </form>
  </div>
  <script src="/javascript/login.js"></script>
</body>

</html>